@model IEnumerable<MyApp.Models.Vehiculo>

@{
    ViewData["Title"] = "Mis Vehículos";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1><i class="bi bi-car-front"></i> Mis Vehículos</h1>
    <a href="@Url.Action("Create")" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> Registrar Nuevo Vehículo
    </a>
</div>

<div class="row">
    @foreach (var vehiculo in Model)
    {
        // Lógica para determinar si hay una reserva activa
        // 💡 CORRECCIÓN APLICADA: Se eliminó el bloque @{ ... } innecesario.
        var reservaActiva = vehiculo.ReservasCupos?.FirstOrDefault();
        var tieneReservaActiva = reservaActiva != null;

        <div class="col-md-6 col-lg-4 mb-3">
            <div class="card h-100">
                <div class="card-header @(vehiculo.Tipo == "Carro" ? "bg-primary" : "bg-info") text-white">
                    <h5 class="mb-0">
                        <i class="bi @(vehiculo.Tipo == "Carro" ? "bi-car-front-fill" : "bi-bicycle")"></i>
                        @vehiculo.Placa
                    </h5>
                </div>
                <div class="card-body">
                    <p class="card-text">
                        <strong>Tipo:</strong>
                        <span class="badge @(vehiculo.Tipo == "Carro" ? "bg-primary" : "bg-info")">
                            @vehiculo.Tipo
                        </span><br>
                        <strong>Marca:</strong> @vehiculo.Marca<br>
                        <strong>Modelo:</strong> @vehiculo.Modelo<br>
                        <strong>Propietario:</strong> @vehiculo.Usuario.Nombre
                    </p>
                </div>
                <div class="card-footer">
                    <div class="d-grid">
                        @if (tieneReservaActiva)
                        {
                            <div class="alert alert-success p-2 mb-2 text-center" role="alert">
                                <i class="bi bi-clock"></i> Reserva Activa hasta:
                                <strong>@reservaActiva.FechaHoraVencimiento.ToString("HH:mm")</strong>
                            </div>
                            <button type="button" class="btn btn-warning" disabled>
                                <i class="bi bi-calendar-check"></i> Ya tienes una Reserva
                            </button>
                        }
                        else
                        {
                            @using (Html.BeginForm("Reservar", "Vehiculos", FormMethod.Post))
                            {
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="id" value="@vehiculo.Id" />
                                <button type="submit" class="btn btn-warning">
                                    <i class="bi bi-calendar-check"></i> Reservar Cupo (30 min)
                                </button>
                            }
                        }
                    </div>
                    <small class="text-muted d-block mt-2">
                        <i class="bi bi-info-circle"></i> Reserva cuando no haya cupos disponibles
                    </small>
                </div>
            </div>
        </div>
    }
</div>

@if (!Model.Any())
{
    <div class="alert alert-info text-center">
        <i class="bi bi-car-front" style="font-size: 3rem;"></i>
        <h4 class="mt-3">No tienes vehículos registrados</h4>
        <p>Registra tu primer vehículo para poder usar el parqueadero.</p>
        <a href="@Url.Action("Create")" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Registrar Mi Primer Vehículo
        </a>
    </div>
}

<div class="card mt-4">
    <div class="card-header bg-light">
        <h5 class="mb-0"><i class="bi bi-info-circle"></i> Información sobre Reservas</h5>
    </div>
    <div class="card-body">
        <ul class="mb-0">
            <li>Las reservas tienen una validez de **30 minutos**</li>
            <li>Solo puedes reservar cuando el parqueadero interno está **lleno** (el sistema lo valida por ti)</li>
            <li>Después de 30 minutos, la reserva se libera automáticamente</li>
            <li>Un funcionario debe autorizar tu ingreso al parqueadero</li>
        </ul>
    </div>
</div>